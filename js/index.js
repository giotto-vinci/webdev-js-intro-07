// Prevent us from attempting to use variables
// that are not declared
"use strict";

/*
function greet(name = "Guest") {
  console.log("Hello, " + name + "!");
}

greet();
greet("Roderick");

function add(num1, num2) {
  return num1 + num2;
}

let sum = add(33, 14);
console.log(sum);

const square = function (number) {
    return number * number;
  };
  
  console.log(square(10));

  function book() {
    console.log("Let's find a good book!");
  }
  const title = (book) => {
    return "This is the great book, " + book + "!";
}


book();


book(title);

title("Meditations");

*/